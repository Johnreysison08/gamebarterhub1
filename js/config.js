// Frontend config: set API base for dev server if running separately
// Example: window.APP_API_BASE = 'http://localhost:4000';
// Defaults to localhost:4000 for local dev. Leave empty to disable remote sync.
window.APP_API_BASE = window.APP_API_BASE || 'http://localhost:4000';
